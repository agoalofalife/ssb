{
  "dependencies": {
    "dotenv": "^4.0.0",
    "nodemon": "^1.14.8",
    "slackbots": "^1.1.0",
    "to": "^0.2.9",
    "update": "^0.7.4"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "codecov": "^3.0.0",
    "faker": "^4.1.0",
    "mocha": "^4.1.0",
    "nyc": "^11.4.1",
    "sinon": "^4.1.3"
  },
  "scripts": {
    "test": "mocha test/**/*.js",
    "coverage": "nyc --reporter=text mocha mocha test/**/*.js",
    "coverage-html": "nyc --reporter=html mocha mocha test/**/*.js",
    "report-coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov"
  }
}
